# TicTacToe API - Environment Variables

# ASP.NET Core Environment
ASPNETCORE_ENVIRONMENT=Production

# Server Configuration
# PORT: The port the application will listen on
PORT=8080

# CORS Configuration (Method 1: Array-based)
# Add your frontend URLs using array syntax
AllowedOrigins__0=https://your-frontend-app.onrender.com
AllowedOrigins__1=https://another-domain.com
# AllowedOrigins__2=https://custom-domain.com

# CORS Configuration (Method 2: Comma-separated, alternative)
# If AllowedOrigins__0 is not set, you can use this instead:
# CORS_ORIGINS=https://your-frontend-app.onrender.com,https://another-domain.com

# Game Settings (optional overrides)
# GameSettings__RoomCodeLength=6
# GameSettings__ReconnectionGracePeriodSeconds=30
# GameSettings__MaxPlayersPerRoom=2
